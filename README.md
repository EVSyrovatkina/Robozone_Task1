# Цифровой двойник сортировочного центра (СЦ) Ozon Tech

Имитационная модель дискретно-событийного моделирования потоков СЦ для Задачи 1.

## Структура проекта
```text
Robozone_Task1/
├── config.py             # Конфигурационный файл с физическими константами СЦ
├── simulation_model.py   # Исполняемый код имитационной модели (SimPy)
└── README.md             # Документация и инструкция по запуску
```

## Описание зависимостей (Dependencies)
Для работы модели требуется интерпретатор Python версии 3.8+ и библиотека дискретно-событийного моделирования `simpy`. Все зависимости зафиксированы в файле `requirements.txt`.

## Инструкция по запуску

### Способ 1: Запуск в облаке через Google Colab (Рекомендуемый)
1. Откройте пустой блокнот на [Google Colab](https://colab.new).
2. В первой ячейке установите библиотеку симуляции, выполнив команду: `!pip install simpy`
3. Создайте новую ячейку, скопируйте туда содержимое файла `simulation_model.py` из этого репозитория и нажмите **Ctrl + Enter**.

### Способ 2: Локальный запуск на ПК
1. Установите зависимости: `pip install -r requirements.txt`
2. Запустите скрипт: `python simulation_model.py`

## Результаты симуляции за 24 часа (86400 секунд)
* **Всего обработано отправлений (КТЯ):** 88 419 шт.
* **Пиковая длина очереди в буфере:** 181 КТЯ.
* **Максимальная утилизация емкости буфера:** 9.78%.
* **Количество критических переполнений конвейера:** 0.
